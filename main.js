// var colors = ["red", "yellow", "orange"];

// alert(colors[2]);

// var colors = new Array("red", "yellow", "orange");

// alert(colors[2]);

// colors.push("green");

// colors.reverse();

// alert(colors[colors.length - 2]);

var promise = {
  var: "text",
  array: [1, 2, 3, 4]
};

alert(promise.var);
